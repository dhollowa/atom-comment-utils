'use babel';

import pipeUtils from '../lib/comment_utils';

describe('CommentUtils', () => {
  describe('when something happens', () => {
    it('compares apples to apples', () => {
      expect("apples").toEqual("apples")
    })
  })
})
